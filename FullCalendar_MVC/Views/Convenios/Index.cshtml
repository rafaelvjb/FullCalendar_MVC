@model IEnumerable<FullCalendar_MVC.Models.Convenio>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ConvenioId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ConvenioId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ConvenioId })
        </td>
    </tr>
}

</table>
